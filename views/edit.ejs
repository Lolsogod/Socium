<% include partials/header %>
<div class="col-md-3 "></div>
<div class="col-res col-md-6 ">
  <!--edit form-->
  <div class="card">
    <div class="card-header">
      <h4>Edit Post</h4>
    </div>
    <div class="card-body">
      <form action="/s/all/<%=post._id%>?_method=PUT" method="POST">
        <input type="text" class="mt-1 form-control" name="Post[title]" value="<%=post.title %>">
        <input type="text" class="mt-1 form-control" name="Post[img]" value="<%=post.img %>">
        <textarea class="mt-1 form-control" name="Post[body]" rows="3"><%=post.body%></textarea>
        <input type="submit" class="ml-2 mt-2 btn btn-primary float-right">
        <a href="/s/all" class="mt-2 btn btn-outline-primary float-right">Cancel</a>
    </div>
  </div>
</div>

</div>
<% include partials/footer %>